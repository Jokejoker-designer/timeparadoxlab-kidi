# Screenshots

Drop annotated UI screenshots here (the build does not generate images
automatically). Suggested captures for the docs:

1. **`dashboard.png`** — the full wide-screen dashboard (controls · 2D slices ·
   3D volume · diagnostics · event log) at the default *Ordinary* phase.
2. **`paradox-3d.png`** — the *Paradox (α=√24, −)* phase: reverse arrival in the
   3D volume with the signal path dipping below the send plane, REVERSE_ARRIVAL in
   the event log highlighted.
3. **`boundary-pulse.png`** — the *Boundary touch (α=5)* phase: the singular planes
   `α=±x` pulsing where `dx²=dα²`, amber SINGULAR_BOUNDARY domain pill.

To capture: open `standalone.html` (or `npm run dev`), pick the phase, press
`Space` to animate, and screenshot. Reference them from the root `README.md`.
